# Django Secret Key - Generate a new one using: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=your_secret_key_here_minimum_50_characters

# Debug mode - Set to False in production
DEBUG=True

# Allowed hosts - Comma-separated list of allowed hostnames
ALLOWED_HOSTS=127.0.0.1,localhost

# Database Configuration
DB_NAME=ders_dagitim_db
DB_USER=postgres
DB_PASSWORD=your_database_password_here
DB_HOST=localhost
DB_PORT=5432

# CORS Configuration - Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
